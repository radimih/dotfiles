---
# Внимание! При одинаковых DEVICE срабатывает только последнее определение
# JOB для данного устройства, вне зависимости от EVENTS.

- JOB: "intercept -g $DEVNODE | space2meta | dual-function-keys -c {{ config_dir }}/dual-function.yaml | uinput -d $DEVNODE"
  DEVICE:
    EVENTS:
      EV_KEY: [KEY_CAPSLOCK, KEY_ENTER, KEY_SPACE]
