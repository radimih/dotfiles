---
galaxy_info:
  author: Radimir Mikhailov
  description: Install bash script for ssh connections
  license: MIT

  min_ansible_version: 2.7

  platforms:
    - name: Debian
      versions:
        - all
    - name: Ubuntu
      versions:
        - all

  galaxy_tags: []

dependencies: []

argument_specs:
  main:
    options:
      connections_dir:
        description: Каталог, где хранятся файлы-профили соединений
        required: true
        type: str
