# Documentation: https://github.com/rvaiya/keyd/blob/master/docs/keyd.scdoc

[ids]

*

[main]

capslock = overload(control, esc)
enter = overload(control, enter)
space = overload(meta, space)
tab = overload(vim_mode, tab)

leftshift = overload(shift, macro(leftalt+f11))
rightshift = overload(shift, macro(leftalt+f12))

[vim_mode]

h = left
j = down
k = up
l = right
